<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>fr.francetelecom.crm</groupId>
	<artifactId>chorniche-angular</artifactId>
	<version>G02R00C00</version>
	<packaging>jar</packaging>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.0.2.RELEASE</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- <dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency> -->
	</dependencies>

	<build>
		<plugins>

			<plugin>
				<groupId>com.github.eirslett</groupId>
				<artifactId>frontend-maven-plugin</artifactId>
				<version>1.6</version>
				<executions>
					<execution>
						<id>install node and npm</id>
						<goals>
							<goal>install-node-and-npm</goal>
						</goals>
						<configuration>
							<nodeVersion>v12.16.3</nodeVersion>
							<npmVersion>6.14.6</npmVersion>
							<nodeDownloadRoot>https://artifactory-iva.si.francetelecom.fr/artifactory/gendevops/node/dist/</nodeDownloadRoot>
							<npmDownloadRoot>https://artifactory-iva.si.francetelecom.fr/artifactory/npmproxy-cache/npm/-/</npmDownloadRoot>
						</configuration>
					</execution>
					<execution>
						<id>npm install</id>
						<goals>
							<goal>npm</goal>
						</goals>

						<!-- optional: default phase is "generate-resources" -->
						<phase>generate-resources</phase>

						<configuration>
							<!-- optional: The default argument is actually "install", so unless
								you need to run some other npm command, you can remove this whole <configuration>
								section. -->
								<arguments>install --registry=https://artifactory-iva.si.francetelecom.fr/artifactory/api/npm/npmproxy</arguments>
						</configuration>
					</execution>

					<execution>
						<id>npm run build</id>
						<goals>
							<goal>npm</goal>
						</goals>
						<configuration>
							<arguments>run build</arguments>
						</configuration>
					</execution>
        
				</executions>
			</plugin>
			
		</plugins>
	</build>
	<distributionManagement>
		<!-- Publish the versioned releases to artifactory  -->
		<repository>
			<id>releases-repo</id>
			<name>parnasse artifactory Repository (RELEASE)</name>
			<url>https://artifactory-iva.si.francetelecom.fr/artifactory/maven-repos-orange-project-parnasse-release</url>
		</repository>

		<!-- Publish the versioned snapshots to artifactory  -->
		<snapshotRepository>
			<id>snapshots-repo</id>
			<name>parnasse artifactory Repository (snapshots)</name>
			<url>https://artifactory-iva.si.francetelecom.fr/artifactory/maven-repos-orange-project-parnasse-snapshots</url>
		</snapshotRepository>



	</distributionManagement>

</project>
