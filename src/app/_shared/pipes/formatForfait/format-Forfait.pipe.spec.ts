import { FormatForfaitPipe } from './format-Forfait.pipe';

describe('FormatForfaitPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatForfaitPipe();
    expect(pipe).toBeTruthy();
  });
});
