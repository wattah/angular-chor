import { AbsNumberFormatterPipe } from './abs-number-formatter.pipe';

describe('AbsNumberFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new AbsNumberFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
