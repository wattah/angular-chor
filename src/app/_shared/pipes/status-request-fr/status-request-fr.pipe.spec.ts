import { StatusRequestFrPipe } from './status-request-fr.pipe';

describe('StatusRequestFrPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusRequestFrPipe();
    expect(pipe).toBeTruthy();
  });
});
