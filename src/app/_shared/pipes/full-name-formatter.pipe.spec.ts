import { FullNameFormatterPipe } from './full-name-formatter.pipe';

describe('FullNameFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new FullNameFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
